# docker-compose.yml
 
# Reference compose file version 3
version: “3”
services:
# Service name is react-app
  react-app:
# build from Dockerfile
    build: .
# expose 80 and 443 as hosting and container ports
    ports:
    - 80:80
    - 443:443
